# FAISReady source-control checkpoint

This checkpoint intentionally excludes live credentials. Secrets belong only in the production host environment.

The complete downloadable build is maintained as `FAISReady-LAUNCH-PACKAGE.zip` in the build workspace. This branch records the deployment configuration, regulatory content policy, pricing and launch gates so deployment can resume safely when Vercel/Supabase are connected.
