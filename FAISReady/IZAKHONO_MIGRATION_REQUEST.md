# FAISReady → IZAKHONO

Requested: 2026-09-12

Primary runtime target: IZAKHONO.
External hosting remains fallback-only while the owner-node/public-ingress and live-payment approval gates are completed.

Migration gate: `FAISReady/.izakhono.json` container build + `/healthz.txt` health check.
