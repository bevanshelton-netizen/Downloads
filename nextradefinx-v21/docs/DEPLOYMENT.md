# V21 controlled-beta deployment

Create dedicated Supabase -> apply prepared migrations -> configure verified-email auth -> add only public project URL/anon key -> run V17 two-user isolation -> record V20 release evidence -> invite one learner -> run V18 -> review with V19 before expansion.

The preview uses local browser demo state only. Real-money features are intentionally absent.
