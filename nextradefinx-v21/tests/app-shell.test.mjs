import test from 'node:test';import assert from 'node:assert/strict';import{nextStep,completion,canEnterPaperPractice}from'../src/app/state-machine.mjs';import{assertProductBoundary,PRODUCT_BOUNDARY}from'../src/app/product-boundary.mjs';import{languageName,isRTL}from'../src/app/languages.mjs';import{validatePublicSupabaseConfig}from'../src/app/supabase-config.mjs';const base={inviteApproved:true,emailVerified:true,termsAccepted:true,privacyAccepted:true,riskAccepted:true,language:'zu',experienceLevel:'beginner',learningGoal:'understand markets',passportCreated:true,firstLessonCompleted:true,paperPracticeCompleted:true,journalCompleted:true};test('new learner starts at invite',()=>assert.equal(nextStep({}),'invite'));test('verified learner without consent goes to consent',()=>assert.equal(nextStep({inviteApproved:true,emailVerified:true}),'consent'));test('complete learner reaches dashboard',()=>assert.equal(nextStep(base),'dashboard'));test('complete learner is 100 percent',()=>assert.equal(completion(base),100));test('paper practice is gated',()=>assert.equal(canEnterPaperPractice({...base,firstLessonCompleted:false}),false));test('real money boundaries remain off',()=>assert.equal(assertProductBoundary(PRODUCT_BOUNDARY).ok,true));test('live execution violation rejected',()=>assert.ok(assertProductBoundary({liveExecution:true}).violations.includes('liveExecution_must_remain_false')));test('language label uses Xitsonga',()=>assert.equal(languageName('ts'),'X'+'itsonga'));test('Arabic is RTL',()=>assert.equal(isRTL('ar'),true));test('safe public config passes',()=>assert.equal(validatePublicSupabaseConfig({NEXT_PUBLIC_SUPABASE_URL:'https://abc123.supabase.co',NEXT_PUBLIC_SUPABASE_ANON_KEY:'anon'}).ready,true));test('service role public leak fails',()=>assert.equal(validatePublicSupabaseConfig({NEXT_PUBLIC_SUPABASE_URL:'https://abc123.supabase.co',NEXT_PUBLIC_SUPABASE_ANON_KEY:'anon',SUPABASE_SERVICE_ROLE_KEY:'secret'}).ready,false));
