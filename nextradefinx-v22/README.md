# NexTradeFinX V22 — One-shot Supabase Bootstrap

V22 consolidates the V15 learner-account and consent migrations into one controlled-beta bootstrap with re-runnable policy setup, RLS, indexes, append-only evidence rules and a read-only verification script.

It contains no credentials and no real-money/brokerage schema.