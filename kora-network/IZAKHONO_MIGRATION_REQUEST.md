# KORA → IZAKHONO

Requested: 2026-09-12

Primary runtime target: IZAKHONO.
External hosting remains fallback-only until owner-node public ingress and production acceptance are green.

Migration gate: `.izakhono.json` container build + `/api/health` health check.
