import {checkInTicket} from './actions';export default async function CheckIn({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){const{error,success}=await searchParams;return <main><section className="subHero"><div className="eyebrow">KORA VENUE CHECK-IN</div><h1>Validate once. Admit once.</h1><p>Scan or enter the ticket reference. A checked-in, void or refunded ticket cannot be reused.</p></section><section><form action={checkInTicket} className="panel formPanel">{error?<p role="alert"><strong>{error}</strong></p>:null}{success?<p role="status"><strong>Ticket admitted successfully.</strong></p>:null}<label>Ticket QR text or reference<input name="ticket_code" required placeholder="KORA-TICKET:..."/></label><button className="primary">Validate and check in</button></form></section></main>}