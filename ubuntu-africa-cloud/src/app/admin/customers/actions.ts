"use server"; import {revalidatePath} from "next/cache";import {requirePlatformAdmin} from "@/lib/auth/require-platform-admin";import {createAdminClient} from "@/lib/supabase/admin";
export async function toggleTenant(fd:FormData){const {user}=await requirePlatformAdmin();const id=String(fd.get("id")??"");const suspend=String(fd.get("suspend"))==="true";const a=createAdminClient();await a.from("tenants").update({is_suspended:suspend}).eq("id",id);await a.from("audit_logs").insert({actor_user_id:user.id,tenant_id:id,action:suspend?"tenant.suspended":"tenant.restored",target_type:"tenant",target_id:id});revalidatePath("/admin/customers");}
