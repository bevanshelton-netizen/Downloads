import { requireUser } from "@/lib/auth/require-user"; import { createContentRequest } from "./actions";
export default async function ContentPage(){const {supabase,user}=await requireUser(); const {data:m}=await supabase.from("tenant_members").select("tenant_id").eq("user_id",user.id).limit(1).maybeSingle(); const {data:items}=m?await supabase.from("content_requests").select("id,request_type,brief,status,response_text,created_at").eq("tenant_id",m.tenant_id).order("created_at",{ascending:false}):{data:[]}; return <main className="mx-auto max-w-4xl px-6 py-12"><h1 className="text-3xl font-bold">AI-assisted content requests</h1><p className="mt-2 text-slate-600">During the zero-cost pilot these requests are handled manually.</p><form action={createContentRequest} className="mt-8 grid gap-3 rounded-2xl border p-6"><select name="type" className="rounded-xl border px-4 py-3"><option>Website content</option><option>Product description</option><option>Business letter</option><option>Social media post</option><option>Advertisement</option></select><textarea required name="brief" placeholder="Describe what you need" className="min-h-32 rounded-xl border px-4 py-3"/><button className="rounded-xl bg-slate-950 px-4 py-3 font-semibold text-white">Send request</button></form><div className="mt-8 grid gap-4">{items?.map(i=><div key={i.id} className="rounded-xl border p-5"><b>{i.request_type}</b><p className="mt-2 text-sm text-slate-600">{i.brief}</p><p className="mt-2 text-xs uppercase">{i.status}</p>{i.response_text&&<div className="mt-4 rounded-xl bg-slate-50 p-4">{i.response_text}</div>}</div>)}</div></main>}
