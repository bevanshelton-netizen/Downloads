import Link from "next/link";import {redirect} from "next/navigation";import {requireUser} from "@/lib/auth/require-user";
const cards=[["Projects","Create and manage your websites","/dashboard/projects"],["Content Requests","Request website copy and marketing support","/dashboard/content"],["Support","Submit and track support tickets","/dashboard/support"],["Billing","Record and review manual EFT payments","/dashboard/billing"]];
export default async function DashboardPage(){const {supabase,user}=await requireUser();const {data:m}=await supabase.from("tenant_members").select("role,tenant_id,tenants(id,name,slug,is_suspended)").eq("user_id",user.id).limit(1).maybeSingle();if(!m)redirect("/onboarding");const tenant:any=m.tenants;return <main className="mx-auto min-h-screen max-w-6xl px-6 py-12"><header className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-sm text-slate-500">{user.email}</p><h1 className="mt-1 text-4xl font-bold">{tenant?.name||"Customer"} dashboard</h1></div><form action="/logout" method="post"><button className="rounded-xl border px-4 py-2">Sign out</button></form></header>{tenant?.is_suspended&&<div className="mt-6 rounded-xl border border-amber-300 bg-amber-50 p-4">This organisation is suspended. Contact support before publishing changes.</div>}<div className="mt-10 grid gap-5 md:grid-cols-2">{cards.map(([t,d,h])=><Link key={t} href={h} className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-semibold">{t}</h2><p className="mt-2 text-slate-600">{d}</p></Link>)}</div></main>}
