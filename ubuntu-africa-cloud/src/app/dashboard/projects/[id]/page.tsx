import Link from 'next/link'; import { notFound } from 'next/navigation'; import { createClient } from '@/lib/supabase/server'; import { getTemplate } from '@/lib/site-templates'; import { submitProjectAction } from './submit-action';
export default async function ProjectPage({params}:{params:Promise<{id:string}>}){const {id}=await params; const supabase=await createClient(); const {data:project}=await supabase.from('projects').select('*').eq('id',id).single(); if(!project)notFound(); const content=project.content as any; const template=getTemplate(project.template_key); return <main className='mx-auto max-w-6xl px-6 py-12'><div className='flex flex-wrap items-start justify-between gap-4'><div><p className='text-sm uppercase tracking-wider text-slate-500'>{template?.name??project.template_key}</p><h1 className='mt-2 text-3xl font-bold'>{project.name}</h1><p className='mt-2 text-slate-600'>Status: {project.status}</p></div><Link href='/dashboard/projects' className='rounded-xl border px-4 py-2'>All projects</Link></div><section className='mt-10 overflow-hidden rounded-3xl border bg-white shadow-sm'><div className='p-10 text-white' style={{backgroundColor:content.primaryColor||'#0f172a'}}><p className='text-sm opacity-80'>Website preview</p><h2 className='mt-3 text-4xl font-bold'>{content.businessName}</h2><p className='mt-3 max-w-2xl text-lg opacity-90'>{content.tagline}</p><span className='mt-6 inline-block rounded-xl px-5 py-3 font-semibold' style={{backgroundColor:content.secondaryColor||'#16a34a'}}>Contact us</span></div><div className='grid gap-10 p-10 md:grid-cols-2'><div><h3 className='text-2xl font-semibold'>About</h3><p className='mt-3 whitespace-pre-wrap text-slate-600'>{content.about}</p></div><div><h3 className='text-2xl font-semibold'>Contact</h3><div className='mt-3 space-y-2 text-slate-600'>{content.email&&<p>{content.email}</p>}{content.phone&&<p>{content.phone}</p>}{content.whatsapp&&<p>WhatsApp: {content.whatsapp}</p>}{content.address&&<p>{content.address}</p>}</div></div></div><div className='border-t p-10'><h3 className='text-2xl font-semibold'>Services / products</h3><div className='mt-5 grid gap-4 md:grid-cols-3'>{(content.services||[]).map((s:any,i:number)=><div key={i} className='rounded-2xl border p-5'><h4 className='font-semibold'>{s.name}</h4><p className='mt-2 text-sm text-slate-600'>{s.description}</p></div>)}</div></div></section>{project.status==='draft'&&<form action={submitProjectAction} className='mt-8'><input type='hidden' name='projectId' value={project.id}/><button className='rounded-xl bg-slate-950 px-6 py-3 font-semibold text-white'>Submit for approval</button></form>}{project.status==='submitted'&&<p className='mt-8 rounded-xl border bg-slate-50 p-4'>This website has been submitted for administrator review.</p>}</main>}
