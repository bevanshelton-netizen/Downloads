import {notFound} from "next/navigation";import {createClient} from "@supabase/supabase-js";
export const revalidate=60;
export default async function PublicSite({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;if(!url||!key)notFound();const s=createClient(url,key);const {data:site}=await s.from("published_sites").select("public_content,template_key").eq("slug",slug).maybeSingle();if(!site)notFound();const c:any=site.public_content;return <main className="min-h-screen bg-white"><section className="px-6 py-24 text-white" style={{backgroundColor:c.primaryColor||"#0f172a"}}><div className="mx-auto max-w-6xl"><p className="text-sm uppercase tracking-widest opacity-70">{site.template_key}</p><h1 className="mt-4 max-w-4xl text-5xl font-bold md:text-7xl">{c.businessName}</h1><p className="mt-5 max-w-2xl text-xl opacity-90">{c.tagline}</p>{c.whatsapp&&<a href={`https://wa.me/${String(c.whatsapp).replace(/\D/g,"")}`} className="mt-8 inline-block rounded-xl px-6 py-3 font-semibold text-white" style={{backgroundColor:c.secondaryColor||"#16a34a"}}>WhatsApp us</a>}</div></section><section className="mx-auto grid max-w-6xl gap-12 px-6 py-16 md:grid-cols-2"><div><h2 className="text-3xl font-bold">About us</h2><p className="mt-4 whitespace-pre-wrap leading-7 text-slate-600">{c.about}</p></div><div><h2 className="text-3xl font-bold">Contact</h2><div className="mt-4 space-y-2 text-slate-600">{c.email&&<p>{c.email}</p>}{c.phone&&<p>{c.phone}</p>}{c.address&&<p>{c.address}</p>}</div></div></section><section className="bg-slate-50 px-6 py-16"><div className="mx-auto max-w-6xl"><h2 className="text-3xl font-bold">What we offer</h2><div className="mt-8 grid gap-5 md:grid-cols-3">{(c.services||[]).map((x:any,i:number)=><article key={i} className="rounded-2xl bg-white p-6 shadow-sm"><h3 className="text-xl font-semibold">{x.name}</h3><p className="mt-2 text-slate-600">{x.description}</p></article>)}</div></div></section><footer className="px-6 py-8 text-center text-sm text-slate-500">Hosted on Ubuntu Africa Cloud</footer></main>}
