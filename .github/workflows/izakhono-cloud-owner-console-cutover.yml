name: IZAKHONO CLOUD Owner Console Cutover

on:
  pull_request:
    paths:
      - 'izakhono-cloud/owner-console-cutover.py'
      - 'izakhono-cloud/alpha-deploy.py'
      - 'izakhono-cloud/build-kora-owner-handoff.py'
      - 'izakhono-cloud/OWNER-CONSOLE-CUTOVER-V1.17.md'
      - 'izakhono-cloud/activate-isn-01.py'
      - 'izakhono-cloud/ISN-01-ACTIVATION.md'
      - 'izakhono-cloud/ACTIVATE-ISN-01-WINDOWS.ps1'
      - 'izakhono-cloud/sovereign-runtime.py'
      - 'izakhono-cloud/edge-quick-proof.py'
      - 'izakhono-cloud/PROVE-ISN-01-EDGE-WINDOWS.ps1'
      - 'START-ISN-01-EDGE-PROOF.cmd'
      - 'START-ISN-01.cmd'
      - 'FAISReady/.izakhono.json'
      - 'FAISReady/Dockerfile'
      - 'FAISReady/healthz.txt'
      - '.github/workflows/izakhono-cloud-owner-console-cutover.yml'

permissions:
  contents: read

jobs:
  cutover-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout reviewed IZAKHONO control plane
        uses: actions/checkout@v4
        with:
          path: control
          persist-credentials: false

      - name: Checkout current application source
        uses: actions/checkout@v4
        with:
          ref: main
          path: apps
          persist-credentials: false

      - name: Checkout current ALLEGRO VIBEZ source
        uses: actions/checkout@v4
        with:
          repository: bevanshelton-netizen/allegro-vibez
          ref: main
          path: allegro
          persist-credentials: false

      - name: Compile cutover orchestrator
        run: |
          python3 -m py_compile control/izakhono-cloud/owner-console-cutover.py
          python3 -m py_compile control/izakhono-cloud/alpha-deploy.py
          python3 -m py_compile control/izakhono-cloud/build-kora-owner-handoff.py
          python3 -m py_compile control/izakhono-cloud/activate-isn-01.py
          python3 -m py_compile control/izakhono-cloud/sovereign-runtime.py
          python3 -m py_compile control/izakhono-cloud/edge-quick-proof.py

      - name: Validate IZAKHONO EDGE proof contract
        run: |
          python3 - <<'PY'
          import importlib.util, json, pathlib, tempfile
          p=pathlib.Path('control/izakhono-cloud/edge-quick-proof.py')
          spec=importlib.util.spec_from_file_location('edgeproof', p)
          m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
          assert m.URL_RE.search('edge=https://abc-123.trycloudflare.com ready')
          with tempfile.TemporaryDirectory() as td:
              td=pathlib.Path(td)
              identity={
                'schema':'izakhono.sovereign-node/v1',
                'node_name':'ISN-01',
                'workload_project':'kora-network',
                'workload_proof':'verified',
                'public_ready':False,
                'commercial_ready':False,
              }
              runtime={
                'schema':'izakhono.sovereign-runtime/v1',
                'node_name':'ISN-01',
                'project':'kora-network',
                'public_scope':'loopback-only',
                'public_ready':False,
                'commercial_ready':False,
                'runtime':{
                  'health_passed':True,
                  'loopback_host':'127.0.0.1',
                  'loopback_port':18080,
                  'health_path':'/api/health',
                },
              }
              ip=td/'identity.json'; rp=td/'runtime.json'
              ip.write_text(json.dumps(identity)); rp.write_text(json.dumps(runtime))
              _,_,url=m.validate_receipts(ip,rp)
              assert url=='http://127.0.0.1:18080/api/health'
          print('IZAKHONO EDGE CONTRACT: PASS')
          PY

      - name: Prove Racing through consolidated path
        run: |
          python3 control/izakhono-cloud/owner-console-cutover.py \
            bevan-shelton-racing/.izakhono.json \
            --repo-root apps \
            --control-root control \
            --ci-proof \
            --receipt-dir /tmp/izakhono-cutover-racing
          APP_MANIFEST='bevan-shelton-racing/.izakhono.json' RECEIPT_DIR='/tmp/izakhono-cutover-racing' python3 - <<'PY'
          import json, os
          root=os.environ['RECEIPT_DIR']
          cutover=json.load(open(root+'/owner-console-cutover-receipt.json'))
          deploy=json.load(open(root+'/deployment-receipt.json'))
          runtime=deploy['execution']['runtime_isolation']
          normalize=lambda values:{str(v).upper().removeprefix('CAP_') for v in values}
          assert cutover['manifest']==os.environ['APP_MANIFEST']
          assert cutover['deployment_health_passed'] is True
          assert cutover['public_ready'] is False
          assert cutover['commercial_ready'] is False
          assert len(cutover['deploy_plan_sha256'])==64
          assert len(cutover['deployment_receipt_sha256'])==64
          assert deploy['execution']['health_passed'] is True
          assert deploy['execution']['health_url_scope']=='loopback-only'
          assert runtime['readonly_rootfs'] is True
          assert runtime['privileged'] is False
          assert 'ALL' in normalize(runtime['cap_drop'])
          assert normalize(runtime['cap_add']) == {'CHOWN','SETUID','SETGID'}
          PY

      - name: Prove current KORA through consolidated path
        run: |
          python3 control/izakhono-cloud/owner-console-cutover.py \
            kora-network/.izakhono.json \
            --repo-root apps \
            --control-root control \
            --ci-proof \
            --receipt-dir /tmp/izakhono-cutover-kora
          APP_MANIFEST='kora-network/.izakhono.json' RECEIPT_DIR='/tmp/izakhono-cutover-kora' python3 - <<'PY'
          import json, os
          root=os.environ['RECEIPT_DIR']
          cutover=json.load(open(root+'/owner-console-cutover-receipt.json'))
          deploy=json.load(open(root+'/deployment-receipt.json'))
          runtime=deploy['execution']['runtime_isolation']
          normalize=lambda values:{str(v).upper().removeprefix('CAP_') for v in values}
          assert cutover['manifest']==os.environ['APP_MANIFEST']
          assert cutover['deployment_health_passed'] is True
          assert cutover['public_ready'] is False
          assert cutover['commercial_ready'] is False
          assert len(cutover['source_commit'])==40
          assert len(cutover['deploy_plan_sha256'])==64
          assert len(cutover['deployment_receipt_sha256'])==64
          assert deploy['execution']['health_passed'] is True
          assert deploy['execution']['health_url_scope']=='loopback-only'
          assert runtime['readonly_rootfs'] is True
          assert runtime['privileged'] is False
          assert 'ALL' in normalize(runtime['cap_drop'])
          assert normalize(runtime['cap_add']) == {'CHOWN','SETUID','SETGID'}
          PY

      - name: Prove KORA persistent ISN-01 runtime
        env:
          IZAKHONO_RUNTIME_STATE_ROOT: /tmp/isn01-runtime-state
        run: |
          printf 'ci-local-ready\n' > /tmp/isn01-local-ready
          python3 control/izakhono-cloud/sovereign-runtime.py deploy \
            kora-network/.izakhono.json \
            --repo-root apps \
            --activation-file /tmp/isn01-local-ready \
            --host-port 18080
          python3 control/izakhono-cloud/sovereign-runtime.py status \
            --project kora-network
          python3 - <<'PY'
          import json
          p='/tmp/isn01-runtime-state/kora-network.json'
          data=json.load(open(p))
          assert data['schema']=='izakhono.sovereign-runtime/v1'
          assert data['node_name']=='ISN-01'
          assert data['project']=='kora-network'
          assert data['runtime']['loopback_port']==18080
          assert data['runtime']['health_passed'] is True
          assert data['runtime']['restart_policy']=='unless-stopped'
          assert data['public_scope']=='loopback-only'
          assert data['public_ready'] is False
          assert data['commercial_ready'] is False
          assert len(data['receipt_sha256'])==64
          print('ISN-01 KORA PERSISTENT RUNTIME: PASS')
          PY
          docker rm -f izakhono-isn-01-kora-network >/dev/null 2>&1 || true

      - name: Prove ALLEGRO VIBEZ through consolidated path
        run: |
          python3 control/izakhono-cloud/owner-console-cutover.py \
            .izakhono.json \
            --repo-root allegro \
            --control-root control \
            --ci-proof \
            --receipt-dir /tmp/izakhono-cutover-allegro
          APP_MANIFEST='.izakhono.json' RECEIPT_DIR='/tmp/izakhono-cutover-allegro' python3 - <<'PY'
          import json, os
          root=os.environ['RECEIPT_DIR']
          cutover=json.load(open(root+'/owner-console-cutover-receipt.json'))
          deploy=json.load(open(root+'/deployment-receipt.json'))
          runtime=deploy['execution']['runtime_isolation']
          normalize=lambda values:{str(v).upper().removeprefix('CAP_') for v in values}
          assert cutover['manifest']==os.environ['APP_MANIFEST']
          assert cutover['deployment_health_passed'] is True
          assert cutover['public_ready'] is False
          assert cutover['commercial_ready'] is False
          assert len(cutover['source_commit'])==40
          assert len(cutover['deploy_plan_sha256'])==64
          assert len(cutover['deployment_receipt_sha256'])==64
          assert deploy['execution']['health_passed'] is True
          assert deploy['execution']['health_url_scope']=='loopback-only'
          assert runtime['readonly_rootfs'] is True
          assert runtime['privileged'] is False
          assert 'ALL' in normalize(runtime['cap_drop'])
          assert normalize(runtime['cap_add']) == {'CHOWN','SETUID','SETGID'}
          PY

      - name: Prove FAISReady through consolidated path
        run: |
          python3 control/izakhono-cloud/owner-console-cutover.py \
            FAISReady/.izakhono.json \
            --repo-root apps \
            --control-root control \
            --ci-proof \
            --receipt-dir /tmp/izakhono-cutover-faisready
          APP_MANIFEST='FAISReady/.izakhono.json' RECEIPT_DIR='/tmp/izakhono-cutover-faisready' python3 - <<'PY'
          import json, os
          root=os.environ['RECEIPT_DIR']
          cutover=json.load(open(root+'/owner-console-cutover-receipt.json'))
          deploy=json.load(open(root+'/deployment-receipt.json'))
          runtime=deploy['execution']['runtime_isolation']
          normalize=lambda values:{str(v).upper().removeprefix('CAP_') for v in values}
          assert cutover['manifest']==os.environ['APP_MANIFEST']
          assert cutover['deployment_health_passed'] is True
          assert cutover['public_ready'] is False
          assert cutover['commercial_ready'] is False
          assert len(cutover['source_commit'])==40
          assert len(cutover['deploy_plan_sha256'])==64
          assert len(cutover['deployment_receipt_sha256'])==64
          assert deploy['execution']['health_passed'] is True
          assert deploy['execution']['health_url_scope']=='loopback-only'
          assert runtime['readonly_rootfs'] is True
          assert runtime['privileged'] is False
          assert 'ALL' in normalize(runtime['cap_drop'])
          assert normalize(runtime['cap_add']) == {'CHOWN','SETUID','SETGID'}
          PY

      - name: Build and exercise offline KORA owner-node handoff
        run: |
          python3 control/izakhono-cloud/build-kora-owner-handoff.py \
            --app-root apps \
            --control-root control \
            --out /tmp/kora-owner-node-handoff.zip
          rm -rf /tmp/kora-owner-handoff-test
          mkdir -p /tmp/kora-owner-handoff-test
          unzip -q /tmp/kora-owner-node-handoff.zip -d /tmp/kora-owner-handoff-test
          cd /tmp/kora-owner-handoff-test/kora-owner-node-handoff
          printf 'ci-ready-marker\n' > /tmp/isn-01-ready
          python3 "$GITHUB_WORKSPACE/control/izakhono-cloud/activate-isn-01.py" \
            --ci-proof \
            --ready-marker /tmp/isn-01-ready \
            --identity-out /tmp/isn-01-sovereign-node.json \
            --handoff "$PWD"
          python3 - <<'PY'
          import json
          p='/tmp/isn-01-sovereign-node.json'
          data=json.load(open(p))
          assert data['schema']=='izakhono.sovereign-node/v1'
          assert data['node_name']=='ISN-01'
          assert data['platform_name']=='IZAKHONO SOVEREIGN NODE'
          assert data['grid_name']=='IZAKHONO SOVEREIGN GRID'
          assert data['owner_console']=='IZAKHONO OWNER CONSOLE'
          assert data['machine_proof_context']=='ci_software_path'
          assert data['workload_project']=='kora-network'
          assert data['workload_proof']=='verified'
          assert data['public_ready'] is False
          assert data['commercial_ready'] is False
          assert len(data['workload_cutover_receipt_sha256'])==64
          print('ISN-01 CI SOFTWARE PATH: PASS')
          PY

      - name: Upload KORA owner-node handoff
        uses: actions/upload-artifact@v4
        with:
          name: kora-owner-node-handoff
          path: /tmp/kora-owner-node-handoff.zip
          if-no-files-found: error
          retention-days: 14


  windows-launcher-syntax:
    runs-on: windows-latest
    steps:
      - name: Checkout reviewed IZAKHONO control plane
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Parse ISN-01 PowerShell without executing it
        shell: powershell
        run: |
          $source = Get-Content 'izakhono-cloud/ACTIVATE-ISN-01-WINDOWS.ps1' -Raw
          [void][ScriptBlock]::Create($source)
          $edge = Get-Content 'izakhono-cloud/PROVE-ISN-01-EDGE-WINDOWS.ps1' -Raw
          [void][ScriptBlock]::Create($edge)
          if ((Get-Content 'START-ISN-01.cmd' -Raw) -notmatch 'd01aa8a5ddb7fae5f656a95dcfdf65cac3a7f37a') {
            throw 'START-ISN-01.cmd is not pinned to the reviewed Windows activator commit'
          }
          if ((Get-Content 'START-ISN-01-EDGE-PROOF.cmd' -Raw) -notmatch '821878ac80392c79a8e1951f00182cc15ddd857c') {
            throw 'START-ISN-01-EDGE-PROOF.cmd is not pinned to the reviewed edge-proof commit'
          }
          Write-Host 'ISN-01 WINDOWS + EDGE LAUNCHER SYNTAX: PASS'
