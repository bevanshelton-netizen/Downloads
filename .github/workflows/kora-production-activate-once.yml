name: KORA One-Time Private Beta Activation — Disabled After Guarded Attempt

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This one-time activation branch is intentionally disabled."
          echo "The guarded attempt on 2026-09-21 stopped before production changes because kora-production secrets/variables were not configured."
          exit 1
