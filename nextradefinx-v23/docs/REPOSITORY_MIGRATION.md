# Repository extraction

Current checkpoints live inside `bevanshelton-netizen/Downloads`. That repository contains unrelated work and should not be the long-term home of NexTradeFinX.

Recommended destination:
- `bevanshelton-netizen/nextradefinx`
- private during controlled beta
- default branch `main`
- pull-request review and passing CI required before merge
- no privileged secrets committed

Historical PRs in Downloads can remain as provenance. V23 is the clean seed for the product repository.
